<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/appBackground">

    <!-- Main Content Area (Paper) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingTop="72dp"
        android:paddingBottom="120dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- Top Bar overlay -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:background="@color/topControlsBackground"
        app:layout_constraintTop_toTopOf="parent"
        android:elevation="12dp"
        android:visibility="visible">

        <!-- Import Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/loadBookBtn"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="📥"
            android:textAllCaps="false"
            android:textColor="@color/primaryText"
            android:textSize="25sp"
            app:iconTint="@color/primaryText" />
        <!-- TOC Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/openTocBtn"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="📑"
            android:textAllCaps="false"
            android:textColor="@color/primaryText"
            android:textSize="25sp"
            app:iconTint="@color/primaryText" />


        <!-- Library Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/openShelfBtn"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="📚"
            android:textAllCaps="false"
            android:textColor="@color/primaryText"
            android:textSize="25sp"
            app:iconTint="@color/primaryText" />

        <!-- Settings Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/openSettingsBtn"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="⚙️"
            android:textAllCaps="false"
            android:textColor="@color/primaryText"
            android:textSize="25sp"
            app:iconTint="@color/primaryText" />

        <!-- Timer Toggle Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/timerToggleButton"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Off"
            android:textAllCaps="false"
            android:textColor="@color/primaryText"
            app:icon="@android:drawable/ic_lock_idle_alarm"
            app:iconSize="32dp"
            app:iconTint="@color/primaryText" />
    </LinearLayout>

    <!-- Bottom Controls overlay -->
    <androidx.cardview.widget.CardView
        android:id="@+id/bottomControls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        app:cardCornerRadius="28dp"
        app:cardElevation="12dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:cardBackgroundColor="@color/topControlsBackground"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            android:padding="8dp">

            <!-- Close Button -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/closeFab"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="4dp"
                android:contentDescription="Close"
                app:backgroundTint="@color/closeButtonColor"
                app:fabSize="mini"
                app:srcCompat="@android:drawable/ic_menu_close_clear_cancel"
                app:tint="@android:color/white" />

            <!-- Rewind Button -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/rewindFab"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="4dp"
                android:contentDescription="Rewind"
                app:backgroundTint="@color/rewindButtonColor"
                app:fabSize="mini"
                app:srcCompat="@android:drawable/ic_media_rew"
                app:tint="@android:color/white" />

            <!-- Play/Pause Button -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/playPauseFab"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="4dp"
                android:contentDescription="Play or Pause"
                app:backgroundTint="@color/playButtonColor"
                app:fabSize="normal"
                app:srcCompat="@android:drawable/ic_media_play"
                app:tint="@android:color/white" />

            <!-- Forward Button -->
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/forwardFab"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="4dp"
                android:contentDescription="Forward"
                app:backgroundTint="@color/rewindButtonColor"
                app:fabSize="mini"
                app:srcCompat="@android:drawable/ic_media_ff"
                app:tint="@android:color/white" />

        </LinearLayout>
    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>
